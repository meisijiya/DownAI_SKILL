---
name: 论文降AI味
description: 中文论文 AI 痕迹检测与改写工具。当用户请求润色、修改、改写、扩写、缩写、降低 AI 率/AI 味/查重率、或要求"不像 AI 写的"任何中文论文内容（摘要、引言、正文、结论、综述、答辩稿、文献综述等）时使用此 skill。基于 8 类中文 AI 痕迹规则与配套检测脚本（scripts/check_ai_tells.py）改写文本，使输出更接近人类学术写作风格。仅限中文论文场景，不要用于英文写作、日常聊天、创意写作或非学术中文。
metadata:
  author: custom
  version: "2.1.0"
---

# 论文降 AI 味 Skill 📝

## 核心原则（先读这一段）

AI 生成的中文论文文本有 8 类高频痕迹。维普、知网等检测平台正是基于这些特征判定 AI 率。改写时**真正改变句式结构**，而不是用同义词替换——后者只会让文本更奇怪。**句式变换 > 同义改写。**

改写前可调用 `scripts/check_ai_tells.py` 做客观检测；改写后再跑一遍验证。脚本输出包含问题位置、严重级别与修正建议。

**最关键的两条铁律：**
1. **句长控制在 25 字以内**（理想 15-22 字）。人类学术写作平均句长约 18 字；AI 输出常达 30-40 字。
2. **每个 AI 痕迹模式都不要反复使用同一种修法**。比如不能用「对…进行…」替代所有「了」——那本身也是 AI 套式。要打散修法。

---

## 何时加载此 Skill

✅ 适用：
- 中文论文片段润色、改写、降重、降 AI 率
- 答辩稿、文献综述等口语转学术
- 长段落缩写、精简、压缩
- 用户输入"把这段改得不像 AI 写的"、"通过维普/知网 AIGC 检测"
- 用户输入"润色/重写这段学术文字"

❌ 不适用：
- 英文论文（用其他工具）
- 日常聊天、创意写作、营销文案
- 翻译任务

---

## 八类 AI 痕迹

每类包含：**特征 → 检测方法 → 修正方法 → 示例**。

### 1️⃣ 「了」滥用（高频致命项）

**特征**：大量 `a研究了b / 分析了 / 探讨了 / 发现了 / 取得了 / 实现了` 句式。

**检测**：正则匹配 `研究|分析|探讨|发现|得到|取得|实现|...` + `了`。

**修正**（轮换使用，避免同一种修法反复出现）：
- 删「了」：`a研究b`（最直接）
- 改主被动：`b的研究由a完成`
- 改变句首：`围绕b，a展开研究`、`就b而言，a的考察结果显示…`
- 用动名词结构：`a的工作聚焦b的考察`

❌ 本文研究了市场现状，分析了用户需求，发现了三个规律。
✓ 本文围绕市场现状展开考察；用户需求分析同步推进；三个核心规律由此揭示。

---

### 2️⃣ 「X 的 Y 与 Z」对偶结构（最高优先级）

**特征**：`前所未有的突破与举世瞩目的成就`、`研究的意义与价值`、`语法与修辞`、`负担与压力`。

**检测**：正则匹配 `xxx的xxx与xxx`。

**修正**：
- 删一个修饰：`研究成果` 而不是 `研究的成果`
- 拆成两句：`取得研究成果。其意义重大。`
- 换连接词：`语法及修辞`、`语法和修辞`
- **避免用"既…也…"替代**（那也是隐性对偶）

❌ 取得了前所未有的研究成果与举世皆惊的研究意义。
✓ 取得若干研究成果，研究意义重大。

❌ 兼顾理论层面的拓展，也关注实践层面的应用价值。
✓ 理论层面有所拓展；实践层面也有应用价值。（仍对偶 → 改为：理论层面已取得拓展；实践应用也初见成效。）

---

### 3️⃣ 前缀形容词堆砌「X 的 Y 的 Z」

**特征**：单句 ≥3 个「的」结构，且多为可省略的定语。

**检测**：单句「的」数量 ≥3 触发警告。

**修正**：能省则省。`在南京的浦口区` → `南京浦口区`；`具有深远意义的重要的影响` → `深远影响`。

❌ 在南京的浦口区，有一条名为凤滁路的赏樱路线，在这条干净整洁的路的两侧……
✓ 凤滁路位于南京浦口区，是当地赏樱路线。每逢春季，两侧樱花盛开。

---

### 4️⃣ 「在 xxx 中」重复

**特征**：同段或相邻句多次 `在xxx中`。

**检测**：单段 ≥2 次触发。

**修正**：
- `就 xxx 而言`、`关于 xxx`、`xxx 方面`
- 句末调整：`xxx 中的 y` → `y 来自 xxx`
- 直接删除介词框架：`在xxx中我们做了y` → `xxx 内做了 y` 或 `围绕 xxx，y 由此展开`

❌ 在微博平台中，用户很多。在小红书平台中，用户更多。在抖音平台中，日活最高。
✓ 微博平台用户基数大；小红书平台增长迅速，用户偏年轻；抖音平台日活最高。

---

### 5️⃣ 超长句与赘述

**特征**：单句 >25 字（需检查），>40 字（必须精简）。

**硬性目标**：
- 理想句长：**15-22 字**
- 单段平均句长：≤ 22 字
- 单段最长句：≤ 30 字（>30 字就拆）

**修正策略**：
- 拆句：找到主谓结构，从中间断开
- 删除无意义修饰：`一定的 / 特定的 / 相关的 / 相应的`
- 用具体数字/事实替代笼统表述

❌ 关于这个问题的相关研究表明，在一定的条件和环境下，通过某种特定的方式和手段，可以取得一定的效果。
✓ 研究表明，特定条件下采用相应方法，可取得效果。（31 字 → 18 字）

❌ 本文就社交媒体对青年用户行为的影响展开探讨，使用习惯与心理特征是核心考察维度。基于调研数据，可归纳出三点规律：社交媒体使用时长的增加往往伴随孤独感上升；平台所采用的算法推荐机制对用户信息获取渠道具有显著作用；不同性别青年对社交媒体的依赖程度差异明显。（首句 22 字，但后续多句 25-30 字）
✓ 本文考察社交媒体对青年行为的影响。数据显示，使用时长与孤独感正相关；算法推荐塑造信息接触范围；性别差异同样显著。

---

### 6️⃣ 句式雷同（对偶/排比）

**特征**：连续多句使用相同结构 `a做了b，c做了d，e做了f`、`建立了xxx，推动了xxx，整合了xxx`。

**检测**：连续 ≥3 句以 `首先/其次/再次/最后/第一/第二` 或相同动词开头。

**修正**：
- 打乱顺序
- 合并或拆分句子
- 改变动词开头
- **避免用"分号 + 短句"组合替代排比**——那仍然是平行结构

❌ 公司建立了合作伙伴关系，推动了产业发展，整合了资源，吸纳了人才。
✓ 公司通过整合资源构建了合作网络；在此基础上吸纳人才，产能稳步提升。

❌ 微博平台用户基数大；小红书平台增长迅速；抖音平台日活最高。（平行句）
✓ 微博平台用户基数最大；小红书在年轻群体中增长迅速；抖音则以日活见长。（句式有差异：基数大/在 X 群体中/以 X 见长）

---

### 7️⃣ AI 套话（套话全家桶）

**特征**：`综上所述 / 值得注意的是 / 不难发现 / 毋庸置疑 / 由此可见 / 总而言之 / 在当今社会 / 基于此 / 据此 / 此外`。

**检测**：正则匹配上述短语。

**修正**：删除套话直接进入实质论述，或用具体数据/事实替代。

❌ 综上所述，深度学习取得了前所未有的突破。值得注意的是，未来仍需进一步研究。
✓ 实验结果显示，模型准确率达 92.7%，但在小样本场景下稳定性不足。

❌ 此外，Transformer 架构也表现优异。（"此外"也是 AI 高频词）
✓ Transformer 架构同样表现优异，在多个基准上效果提升。

---

### 8️⃣ 口语化与模糊表达

**特征**：`其实 / 就是 / 挺 / 蛮 / 差不多 / 可能 / 应该 / 还行 / 我觉得 / 我做的` 出现在学术正文中。

**修正**：
- 删或换成书面语：`实际上 / 即为 / 较为 / 大致`
- 第一人称口语 → 学术主语：`我们做了` → `本文/本研究`
- 模糊判断 → 具体表述：`效果还可以` → `准确率达 XX%`

❌ 这个方法其实挺好的，就是可能有点贵。
✓ 该方法具备优势，但成本偏高。

❌ 我们做了三件事：第一，我做了一个改进的网络；第二，我做了实验；第三，我把它用到了医院数据上。
✓ 本文工作包含三部分：网络架构改进、对比实验验证、医院数据落地测试。

---

## 场景化改写指引

### 场景 A：单段润色

1. 通读原文，标注触发痕迹的位置
2. 逐句改写，控制平均句长 15-22 字
3. 删除 AI 套话
4. 跑脚本验证 high 严重度问题归零

### 场景 B：多段落改写

1. 整体先读一遍，把握段落逻辑
2. 段间过渡使用不同连接方式（背景→问题→方法→结果→意义），避免每段都以"在当前…中"开头
3. 每段独立做"场景 A"的处理
4. 整体跑脚本验证

### 场景 C：答辩稿 → 学术书面语

1. **删除口语化词**：其实/挺/蛮/可能/还行/我觉得 → 直接删除或换书面语
2. **第一人称改写**：
   - `我们做了` → `本文/本研究`
   - `我觉得` → 删除或改为客观陈述
   - `我想说` → 删除
3. **模糊判断 → 具体表述**：
   - `效果还可以` → `准确率达 XX%`
   - `有一些问题` → 具体列问题
4. **重组句式**：口语稿常有"其实…然后…就是…" 流水句，拆开重组
5. 跑脚本验证

### 场景 D：缩写（压缩 50%）

1. **保留核心信息**：删去重复解释和铺垫
2. **合并同类项**：把多条"展开了…开展了…"合并为一句
3. **删除套话**：综上所述/值得注意的是/此外 全删
4. **避免"既…又…"结构**（缩写时容易写出对偶）
5. 跑脚本验证，并检查长度比例

---

## 正向范文：人类风格学术段落

**示例 1（实证研究类）：**
> 本研究在三个城市发放问卷 1,247 份，回收有效问卷 1,108 份。统计结果显示，使用时长与孤独感呈正相关（r = 0.34, p < 0.01），这一关联在 18-24 岁群体中尤为突出。访谈材料进一步表明，算法推荐塑造了用户的信息接触范围，但并非所有受访者都对此保持警惕。

**示例 2（综述类）：**
> 既有研究对社交媒体影响的讨论大致可分为两类：一类聚焦使用时长与心理健康的关系，另一类关注算法推荐如何作用于信息接触。前者多采用问卷法，后者以行为日志分析为主。然而，两类研究很少交叉分析，导致对"使用时长—算法接触—心理反应"的整体机制理解不足。

**示例 3（答辩稿 → 学术书面语）：**
> 原文口语：「我做的这个东西其实还可以，就是数据少的时候不太行」
>
> 学术改写：本研究所提方法在标准数据集上效果良好；但当训练样本规模缩小时，性能下降明显，这反映出模型对小样本场景的适应能力不足。

---

## 使用流程（每次润色时执行）

1. **检测原文**：跑 `python scripts/check_ai_tells.py --file <原文路径>` 或 `--text`，记录 high 严重度数量
2. **改写**：根据 8 类规则 + 场景化指引做句式层面的修改（不是同义词替换）
3. **复测**：再跑一次脚本，确认 high 严重度问题归零
4. **人工抽查**：通读一遍确认语义未变、读起来像人写的

---

## 自检清单（输出前快速过一遍）

- [ ] 「了」不连续出现，全文 ≤ 1 次
- [ ] 「X 的 Y 与 Z」结构没有出现
- [ ] 单句「的」<3 个
- [ ] 「在 xxx 中」不在同段重复
- [ ] 平均句长 ≤ 22 字，最长句 ≤ 30 字
- [ ] 没有连续 3+ 句相同句式开头
- [ ] 没有 AI 套话（综上所述/此外/值得注意的是/不难发现/首先...其次...最后）
- [ ] 没有口语化词（其实/就是/挺/还行）
- [ ] 没有第一人称口语（我觉得/我做的/我想说）

---

## 检测平台差异

| 平台 | 严格度 | 应对 |
|------|:------:|------|
| 知网 AIGC | ⭐⭐⭐ | 修正 1-6 类即可 |
| 维普 AIGC | ⭐⭐⭐⭐⭐ | 需全面修正 1-8 类，句式变化与句长控制是核心 |
| PaperPass | ⭐⭐⭐⭐ | 关注「了」与「X 的 Y 与 Z」 |

---

## 自相矛盾的检查清单（改写时避免）

- ❌ 把所有「了」改成「对…进行…」→ 这本身也是 AI 痕迹
- ❌ 把"前所未有的突破与举世瞩目的成就"改成"既…也…" → 仍是对偶
- ❌ 把"在X中"全部改成"关于X" → 也要避免重复使用同一替换
- ❌ 把所有"首先/其次/最后"改成"第一/第二/第三" → 仍是排比
- ❌ 用分号连接多个短句 → 仍是平行结构
- ❌ 用同义词替换所有重复词 → AI 检测看的是句式不是词频

**核心策略：每种修法用一次就换下一种，目标是让输出句式看起来"非模式化"。**

---

> 🎓 脚本可独立运行：`python scripts/check_ai_tells.py --file your_paper.txt --json`
